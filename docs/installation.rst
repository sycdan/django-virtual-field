============
Installation
============

At the command line::

    $ easy_install django-virtual-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-virtual-field
    $ pip install django-virtual-field
